{
  "name": "gemini-runtime-mcp",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "planner-tools",
    "researcher-tools",
    "executor-tools",
    "verifier-tools"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present"
  }
}
